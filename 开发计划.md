好的，全部确认。

我们采用“分阶段实现、每阶段确认后继续”的方式，不一次性铺开全部工程能力。每个阶段都会控制代码量，并在完成后提供：

- 本阶段完成内容
- 修改文件
- 验证结果
- 已知问题或待决策项
- 下一阶段计划

建议分为以下阶段：

1. 基础规范与 Monorepo 骨架  
   包括目录结构、`AGENTS.md`、pnpm workspace、Node/pnpm 版本约束、基础文档目录和根配置。

2. Vue 应用最小闭环  
   包括 Vue 3、TypeScript、Router、Pinia、SCSS、基础页面和“功能待开发”入口。

3. Vite 正式构建链路  
   先完成默认的 Vite 开发、生产构建、环境配置、资源处理和基础代码分割。

4. 共享 Packages  
   分阶段加入 `config`、`types`、`shared`、`http`、`websocket`、`monitoring`、`ui`。

5. Webpack 5 正式构建链路  
   在复用同一份业务源码的前提下，逐步加入 loader、plugin、cache、thread-loader、SplitChunks 和生产优化。

6. 工程质量与测试  
   包括 ESLint、Prettier、Stylelint、Husky、lint-staged、Playwright Smoke Test。

7. 分析、Benchmark 与部署  
   包括 Bundle Analysis、Vite/Webpack 构建对比、GitHub Actions、GitHub Pages、Docker 验证。

第一阶段我建议只做“基础规范与 Monorepo 骨架”，暂不写具体 Demo，也暂不配置完整的 Vite/Webpack 复杂逻辑。完成后先由你确认目录和工程边界，再进入第二阶段。