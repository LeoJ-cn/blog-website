import { CompilerPackageNames } from "./shared";

export default {
  bindingEnabled: true,
  compilerPackageName: "typescript" as CompilerPackageNames,
  scriptKind: 4,
  scriptTarget: 99,
  showFactoryCode: false,
  showInternals: false,
  treeMode: 0 // Tree Mode设置： 默认node.forEach
}